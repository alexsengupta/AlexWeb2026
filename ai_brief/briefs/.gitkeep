# Downloaded briefs land here. The files themselves are gitignored; this file
# keeps the directory present in the repo so the fetch script and the web
# server always have somewhere to write and read.
